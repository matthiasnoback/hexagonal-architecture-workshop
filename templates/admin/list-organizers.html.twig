{% extends "@layout/default.html.twig" %}

{% block title %}Organizers{% endblock %}

{% block content %}
    <h1>Organizers</h1>

    {% if organizers is empty %}
    <p>There are no organizers</p>
    {% else %}
        <table class="table table-striped">
            <thead>
            <tr><th>Name</th><th>Actions</th></tr>
            </thead>
            <tbody>
        {% for organizer in organizers %}
            <tr>
                <td>{{ organizer.name }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('create_invoice', { organizerId: organizer.userId }) }}">Create invoice</a>
                    <a class="btn btn-primary" href="{{ path('list_invoices', { organizerId: organizer.userId }) }}">List invoices</a>
                </td>
            </tr>
            {% endfor %}
            </tbody>
        </table>
    {% endif %}

{% endblock %}
